import 'package:flutter/material.dart';

class Invite {
  final String title;
  final String subTitle;
  final String image;
  final Color color;

  Invite({
    required this.title,
    required this.subTitle,
    required this.image,
    required this.color,
  });
}
